import { createFileRoute, Link } from "@tanstack/react-router";
import { useQuery, useServerFn } from "@tanstack/react-router" as any; // placeholder
export const Route = createFileRoute("/_authenticated/dashboard")({ component: () => null });
